/**
 * PrimeNG-dependent components
 * These components require PrimeNG to be configured in the consuming application
 * Only import from this file in applications that have PrimeNG set up (e.g., Shell)
 */

export * from './lib/language-switch/language-switch';
